# System Architecture - Telegram Quiz NFT Platform

## 🏗️ High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         USERS                                    │
│                  (Telegram Clients)                              │
└────────────┬────────────────────────────┬───────────────────────┘
             │                            │
             │ Telegram API               │ HTTPS
             │                            │
┌────────────▼───────────┐    ┌──────────▼────────────────────────┐
│   Telegram Bot         │    │   Web Frontend (React)            │
│   (Aiogram 3)          │    │   - Admin Panel                   │
│                        │    │   - User WebApp                   │
│   Handlers:            │    │                                   │
│   - /start             │    │   Features:                       │
│   - /quiz              │    │   - Quiz CRUD                     │
│   - /mynfts            │    │   - Analytics                     │
│   - Quiz flow          │    │   - NFT Gallery                   │
│   - NFT commands       │    │   - TON Connect                   │
└────────────┬───────────┘    └──────────┬────────────────────────┘
             │                           │
             │ HTTP API                  │ HTTP API
             │                           │
┌────────────┴───────────────────────────┴────────────────────────┐
│                   Backend API (FastAPI)                          │
│                                                                  │
│   ┌──────────────────────────────────────────────────────┐     │
│   │              API Routes (v1)                          │     │
│   │  /quizzes  /users  /nft  /auth  /admin  /webhooks   │     │
│   └──────────────────┬───────────────────────────────────┘     │
│                      │                                           │
│   ┌──────────────────▼───────────────────────────────────┐     │
│   │              Business Logic Layer                     │     │
│   │                                                       │     │
│   │  ┌─────────────┐  ┌──────────────┐  ┌────────────┐  │     │
│   │  │Quiz Service │  │ NFT Service  │  │Auth Service│  │     │
│   │  └─────────────┘  └──────────────┘  └────────────┘  │     │
│   │                                                       │     │
│   │  ┌─────────────┐  ┌──────────────┐  ┌────────────┐  │     │
│   │  │User Service │  │ TON Service  │  │Stats Svc   │  │     │
│   │  └─────────────┘  └──────────────┘  └────────────┘  │     │
│   └────────────┬──────────────────────────┬──────────────┘     │
│                │                          │                     │
└────────────────┼──────────────────────────┼─────────────────────┘
                 │                          │
    ┌────────────▼────────┐    ┌───────────▼──────────────┐
    │   PostgreSQL DB     │    │   TON Blockchain         │
    │                     │    │                          │
    │   Tables:           │    │   - NFT Collection       │
    │   - users           │    │   - Mint Transactions    │
    │   - quizzes         │    │   - User Wallets         │
    │   - questions       │    │                          │
    │   - answers         │    │   Network: Mainnet       │
    │   - quiz_results    │    │   Standard: NFT-721      │
    │   - mint_txs        │    └──────────────────────────┘
    │   - nft_metadata    │
    └─────────────────────┘
                                ┌──────────────────────────┐
    ┌─────────────────────┐    │   IPFS / TON Storage     │
    │   Redis Cache       │    │                          │
    │                     │    │   - NFT Images           │
    │   - User sessions   │    │   - Metadata JSON        │
    │   - Quiz state      │    │   - Result images        │
    │   - Rate limiting   │    └──────────────────────────┘
    └─────────────────────┘
```

---

## 🔧 Technology Stack

### Backend
| Component       | Technology | Version | Purpose              |
|-----------------|------------|---------|----------------------|
| Bot Framework   | Aiogram    | 3.x     | Telegram bot interactions |
| API Framework   | FastAPI    | 0.104+  | REST API server      |
| Language        | Python     | 3.12    | Core language        |
| Package manager | uv         | latest  | Manage project       |
| Async Runtime   | asyncio    | stdlib  | Async operations     |
| Validation      | Pydantic   | 2.x     | Data validation      |
| ORM             | SQLAlchemy | 2.x     | Database ORM         |
| Migrations      | Alembic    | latest  | DB schema management |

### Database & Cache
| Component | Technology    | Purpose |
|-----------|---------------|---------|
| Primary DB | PostgreSQL 17 | Main data storage |
| Cache | Redis 8       | Session & query caching |
| Connection Pool | asyncpg       | Async PostgreSQL driver |

### Blockchain
| Component | Technology | Purpose |
|-----------|-----------|---------|
| Blockchain | TON | NFT hosting |
| SDK | pytoniq / tonapi | TON interaction |
| Storage | IPFS or TON Storage | NFT metadata & images |
| Wallet Integration | TON Connect 2.0 | User wallet connection |

### Frontend
| Component | Technology | Version | Purpose |
|-----------|-----------|---------|---------|
| Framework | React | 18+ | UI framework |
| Language | TypeScript | 5+ | Type safety |
| Build Tool | Vite | 5+ | Fast builds |
| UI Library | Material-UI / Tailwind | latest | Component library |
| State | React Query + Context | latest | State management |
| Telegram SDK | @twa-dev/sdk | latest | WebApp integration |

### DevOps
| Component | Technology           | Purpose |
|-----------|----------------------|---------|
| Containerization | Docker + Compose     | Environment isolation |
| Reverse Proxy | Traefik              | Load balancing |
| CI/CD | GitHub Actions       | Automation |
| Monitoring | Prometheus + Grafana | Metrics |
| Logging | Loguru               | Structured logging |
| Error Tracking | Sentry               | Error monitoring |

---

## 📊 Database Schema

### Entity Relationship Diagram

```
┌─────────────────────┐
│      users          │
├─────────────────────┤
│ id (PK)             │
│ telegram_id (UQ)    │───┐
│ username            │   │
│ first_name          │   │
│ last_name           │   │
│ is_admin            │   │
│ created_at          │   │
│ updated_at          │   │
└─────────────────────┘   │
                          │
                          │
┌─────────────────────┐   │     ┌─────────────────────┐
│     quizzes         │   │     │   quiz_results      │
├─────────────────────┤   │     ├─────────────────────┤
│ id (PK)             │   │     │ id (PK)             │
│ title               │   │     │ user_id (FK) ───────┘
│ description         │   │     │ quiz_id (FK) ───┐
│ image_url           │   │     │ answers_data        │
│ is_active           │   │     │ result_type         │
│ created_by (FK)     │   │     │ score               │
│ created_at          │   │     │ nft_minted          │
│ updated_at          │   │     │ nft_address         │
└──────┬──────────────┘   │     │ completed_at        │
       │                  │     └─────────┬───────────┘
       │                  │               │
       │                  │               │
┌──────▼──────────────┐   │     ┌─────────▼───────────┐
│    questions        │   │     │  mint_transactions  │
├─────────────────────┤   │     ├─────────────────────┤
│ id (PK)             │   │     │ id (PK)             │
│ quiz_id (FK)        │   │     │ result_id (FK)      │
│ text                │   │     │ user_id (FK) ───────┘
│ order_index         │   │     │ nft_address         │
│ created_at          │   │     │ transaction_hash    │
└──────┬──────────────┘   │     │ status              │
       │                  │     │ metadata_uri        │
       │                  │     │ error_message       │
┌──────▼──────────────┐   │     │ created_at          │
│     answers         │   │     │ confirmed_at        │
├─────────────────────┤   │     └─────────────────────┘
│ id (PK)             │   │
│ question_id (FK)    │   │
│ text                │   │     ┌─────────────────────┐
│ result_type         │   │     │   nft_metadata      │
│ weight              │   │     ├─────────────────────┤
│ order_index         │   │     │ id (PK)             │
└─────────────────────┘   │     │ quiz_id (FK) ───────┘
                          │     │ result_type         │
┌─────────────────────┐   │     │ name                │
│  result_types       │   │     │ description         │
├─────────────────────┤   │     │ image_url           │
│ id (PK)             │   │     │ attributes_json     │
│ quiz_id (FK) ───────┘        │ created_at          │
│ type_key            │          └─────────────────────┘
│ title               │
│ description         │
│ image_url           │
└─────────────────────┘
```

### Key Relationships

1. **users ↔ quizzes**: One-to-many (creator relationship)
2. **users ↔ quiz_results**: One-to-many (participation)
3. **quizzes ↔ questions**: One-to-many (quiz structure)
4. **questions ↔ answers**: One-to-many (question options)
5. **quizzes ↔ result_types**: One-to-many (possible outcomes)
6. **quiz_results ↔ mint_transactions**: One-to-one (NFT minting)
7. **quizzes ↔ nft_metadata**: One-to-many (NFT templates)

---

## 🔄 Key Workflows

### 1. User Takes Quiz Flow

```
┌─────────┐
│  User   │
│ /start  │
└────┬────┘
     │
     ▼
┌─────────────────────┐
│  Bot: Show Quiz     │
│  Selection Menu     │
└────┬────────────────┘
     │ User selects quiz
     ▼
┌─────────────────────┐
│  Bot: Load          │
│  Questions          │
└────┬────────────────┘
     │
     ▼
┌─────────────────────┐
│  Bot: Show Q1       │
│  with Inline        │
│  Keyboard           │
└────┬────────────────┘
     │ User answers
     ▼
┌─────────────────────┐
│  Store Answer       │
│  in Redis Session   │
└────┬────────────────┘
     │
     ▼
┌─────────────────────┐
│  Next Question?     │
└────┬────────────────┘
     │ Yes: Repeat
     │ No: Calculate
     ▼
┌─────────────────────┐
│  Calculate Result   │
│  Type & Score       │
└────┬────────────────┘
     │
     ▼
┌─────────────────────┐
│  Save to DB         │
│  (quiz_results)     │
└────┬────────────────┘
     │
     ▼
┌─────────────────────┐
│  Display Result     │
│  + "Mint NFT" BTN   │
└─────────────────────┘
```

### 2. NFT Minting Flow

```
┌─────────────────────┐
│  User clicks        │
│  "Mint NFT"         │
└────┬────────────────┘
     │
     ▼
┌─────────────────────┐
│  Check if already   │
│  minted             │
└────┬────────────────┘
     │ Not minted
     ▼
┌─────────────────────┐
│  Payment required?  │
└────┬────────────────┘
     │ Yes
     ▼
┌─────────────────────┐
│  Show Payment       │
│  Options:           │
│  - TON              │
│  - Telegram Stars   │
└────┬────────────────┘
     │ User pays
     ▼
┌─────────────────────┐
│  Verify Payment     │
│  via Webhook        │
└────┬────────────────┘
     │ Confirmed
     ▼
┌─────────────────────┐
│  Generate NFT       │
│  Metadata           │
└────┬────────────────┘
     │
     ▼
┌─────────────────────┐
│  Upload Image +     │
│  JSON to IPFS       │
└────┬────────────────┘
     │
     ▼
┌─────────────────────┐
│  Call TON SDK       │
│  mint_nft()         │
└────┬────────────────┘
     │
     ▼
┌─────────────────────┐
│  Create mint_tx     │
│  record (pending)   │
└────┬────────────────┘
     │
     ▼
┌─────────────────────┐
│  Wait for           │
│  confirmation       │
└────┬────────────────┘
     │
     ▼
┌─────────────────────┐
│  Update status      │
│  to "completed"     │
└────┬────────────────┘
     │
     ▼
┌─────────────────────┐
│  Notify user        │
│  + Share NFT link   │
└─────────────────────┘
```

### 3. Admin Creates Quiz Flow

```
┌─────────────────────┐
│  Admin logs in      │
│  via Telegram       │
└────┬────────────────┘
     │
     ▼
┌─────────────────────┐
│  Verify admin       │
│  role in DB         │
└────┬────────────────┘
     │
     ▼
┌─────────────────────┐
│  Show Admin         │
│  Dashboard          │
└────┬────────────────┘
     │
     ▼
┌─────────────────────┐
│  Click "New Quiz"   │
└────┬────────────────┘
     │
     ▼
┌─────────────────────┐
│  Fill Basic Info    │
│  - Title            │
│  - Description      │
│  - Image            │
└────┬────────────────┘
     │
     ▼
┌─────────────────────┐
│  Add Questions      │
│  (repeatable)       │
└────┬────────────────┘
     │
     ▼
┌─────────────────────┐
│  For each question: │
│  - Add answers      │
│  - Map to results   │
└────┬────────────────┘
     │
     ▼
┌─────────────────────┐
│  Define Result      │
│  Types & NFTs       │
└────┬────────────────┘
     │
     ▼
┌─────────────────────┐
│  Preview Quiz       │
└────┬────────────────┘
     │
     ▼
┌─────────────────────┐
│  Save to DB         │
│  (transaction)      │
└────┬────────────────┘
     │
     ▼
┌─────────────────────┐
│  Quiz now live!     │
│  Users can take it  │
└─────────────────────┘
```

---

## 🔐 Security Architecture

### Authentication Layers

1. **Telegram Bot**
   - Telegram User ID verification
   - No separate auth needed (Telegram handles it)

2. **Web Frontend**
   - Telegram Login Widget verification
   - JWT tokens for API access
   - HMAC signature validation

3. **WebApp**
   - `initData` signature validation
   - Short-lived session tokens

### Security Measures

```
┌─────────────────────────────────────────────┐
│         Security Layers                      │
├─────────────────────────────────────────────┤
│  1. Input Validation (Pydantic)             │
│     - Type checking                          │
│     - Length limits                          │
│     - Format validation                      │
├─────────────────────────────────────────────┤
│  2. Authentication                           │
│     - Telegram signature verification       │
│     - JWT token validation                  │
│     - Role-based access control             │
├─────────────────────────────────────────────┤
│  3. Rate Limiting (Redis)                   │
│     - Per-user limits                       │
│     - Per-endpoint limits                   │
│     - DDoS protection                       │
├─────────────────────────────────────────────┤
│  4. SQL Injection Prevention                │
│     - Parameterized queries (SQLAlchemy)    │
│     - ORM usage                             │
├─────────────────────────────────────────────┤
│  5. XSS Prevention                          │
│     - React automatic escaping              │
│     - Content Security Policy headers       │
├─────────────────────────────────────────────┤
│  6. CORS Configuration                      │
│     - Whitelist origins                     │
│     - Credential handling                   │
├─────────────────────────────────────────────┤
│  7. Secrets Management                      │
│     - Environment variables                 │
│     - No hardcoded secrets                  │
│     - .env files gitignored                 │
├─────────────────────────────────────────────┤
│  8. HTTPS Everywhere                        │
│     - TLS 1.3                               │
│     - SSL certificates                      │
└─────────────────────────────────────────────┘
```

---

## 📈 Scalability Considerations

### Horizontal Scaling

```
┌──────────────────────────────────────────────┐
│           Load Balancer (NGINX)              │
└─────────────────┬────────────────────────────┘
                  │
        ┌─────────┴─────────┐
        │                   │
┌───────▼──────┐    ┌───────▼──────┐
│ API Server 1 │    │ API Server 2 │
│ (FastAPI)    │    │ (FastAPI)    │
└───────┬──────┘    └───────┬──────┘
        │                   │
        └─────────┬─────────┘
                  │
        ┌─────────▼──────────┐
        │  PostgreSQL        │
        │  (with replicas)   │
        └────────────────────┘
```

### Caching Strategy

1. **Redis Cache Layers**
   - User session data (TTL: 1 hour)
   - Quiz data (TTL: 5 minutes)
   - API responses (TTL: 1 minute)
   - Rate limit counters (TTL: varies)

2. **Database Query Optimization**
   - Indexes on foreign keys
   - Composite indexes for common queries
   - Query result caching
   - Connection pooling

### Performance Targets

| Metric | Target | Measurement |
|--------|--------|-------------|
| API Response Time | < 200ms (p95) | Prometheus |
| Bot Response Time | < 1s | Custom metrics |
| Database Query Time | < 50ms (p95) | pganalyze |
| NFT Mint Time | < 30s | Transaction logs |
| Frontend Load Time | < 2s | Lighthouse |
| Uptime | > 99.9% | UptimeRobot |

---

## 🔌 API Design

### REST API Structure

```
/api/v1
├── /auth
│   ├── POST /login          # Telegram login
│   └── POST /refresh        # Refresh token
├── /quizzes
│   ├── GET    /             # List all quizzes
│   ├── POST   /             # Create quiz (admin)
│   ├── GET    /{id}         # Get quiz details
│   ├── PUT    /{id}         # Update quiz (admin)
│   ├── DELETE /{id}         # Delete quiz (admin)
│   └── POST   /{id}/submit  # Submit quiz answers
├── /users
│   ├── GET /me              # Current user profile
│   ├── GET /me/results      # My quiz results
│   ├── GET /me/nfts         # My NFTs
│   └── GET /stats           # User statistics
├── /nft
│   ├── POST /mint           # Mint NFT
│   ├── GET  /{address}      # NFT details
│   └── GET  /metadata/{id}  # NFT metadata
├── /admin
│   ├── GET /users           # List users
│   ├── GET /analytics       # Platform analytics
│   └── POST /users/{id}/role # Update user role
└── /webhooks
    ├── POST /ton            # TON payment callback
    └── POST /stars          # Telegram Stars callback
```

### WebSocket Endpoints (Optional)

```
/ws/quiz/{quiz_id}        # Real-time quiz session
/ws/mint/{transaction_id} # NFT minting status updates
```

---

## 🧪 Testing Strategy

### Test Pyramid

```
       ┌───────────┐
       │    E2E    │  (5% - Critical paths)
       └───────────┘
      ┌─────────────┐
      │ Integration │  (25% - API + DB)
      └─────────────┘
     ┌───────────────┐
     │  Unit Tests   │   (70% - Business logic)
     └───────────────┘
```

### Testing Layers

1. **Unit Tests** (pytest)
   - Service layer logic
   - Utility functions
   - Data validation

2. **Integration Tests** (pytest + httpx)
   - API endpoint testing
   - Database operations
   - External service mocks

3. **E2E Tests** (Playwright)
   - User quiz flow
   - Admin quiz creation
   - NFT minting flow

4. **Load Tests** (Locust)
   - Concurrent users
   - API throughput
   - Database performance

---

## 📦 Deployment Architecture

### Docker Compose Services

```yaml
services:
  postgres:
    image: postgres:17
    volumes:
      - pg_data:/var/lib/postgresql/data

  redis:
    image: redis:8-alpine

  backend:
    build: ./backend
    depends_on:
      - postgres
      - redis
    environment:
      - DATABASE_URL
      - REDIS_URL
      - BOT_TOKEN

  bot:
    build: ./backend
    command: python -m app.bot
    depends_on:
      - postgres
      - redis

  frontend:
    build: ./frontend
    ports:
      - "3000:80"

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
      - ./ssl:/etc/nginx/ssl
```

---

## 🎯 Conclusion

This architecture provides:
- **Scalability**: Horizontal scaling for API and bot
- **Security**: Multiple layers of validation and auth
- **Performance**: Caching and optimization strategies
- **Maintainability**: Clear separation of concerns
- **Testability**: Comprehensive testing strategy

The modular design allows incremental development and easy feature additions without major refactoring.
